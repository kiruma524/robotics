Нужно было развить two_link.py, добавить третий джоинт в начало кинематической цепочки (с поворотом вокруг оси Z) и то же самое движение в заданную декартову точку (уже 3D), но через встроенную функцию calculateJacobian
